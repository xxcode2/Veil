[package]
name = "veil-voting-mpc"
version = "0.1.0"
edition = "2021"

[dependencies]
arcium-sdk = "0.2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib", "lib"]

[profile.release]
opt-level = 3
lto = true
